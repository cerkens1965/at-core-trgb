name: Build AT-VIEW (PlatformIO)

on:
  workflow_dispatch:
  pull_request:
  push:
    paths:
      - "src/**"
      - "examples/at_core_debug/**"
      - ".github/workflows/pio.yml"
      - "platformio.ini"

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/cache@v4
        with:
          path: |
            ~/.cache/pip
            ~/.platformio
          key: ${{ runner.os }}-pio-${{ hashFiles('platformio.ini') }}

      - name: Install PlatformIO
        run: pip install --upgrade platformio

      # (2026-08-05) Périmètre écran = WS-241 UNIQUEMENT (décision Christophe 03/08) —
      # T-RGB/T4-S3/WS-216 sont GELÉS et ne compilent plus : le CI ne builde que la cible réelle.
      - name: Build at_core_debug
        run: pio run -e WS-241
        env:
          PLATFORMIO_SRC_DIR: ${{ github.workspace }}/examples/at_core_debug
